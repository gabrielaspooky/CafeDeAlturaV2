/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'], // Agrega aquí los dominios que permitirás
    },
  };
  
  export default nextConfig;
  